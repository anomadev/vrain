En PHP, las **funciones** pueden recibir datos a través de parámetros declarados en su firma y valores pasados mediante argumentos al llamar (`positional arguments`), los cuales se **evalúan de izquierda a derecha** antes de la invocación [PHP](https://www.php.net/manual/en/functions.arguments.php). PHP soporta:
- **Paso por valor** (comportamiento por defecto) y **paso por referencia** con `&` para que la función modifique la variable original.
- **Valores por defecto** para parámetros, útiles cuando el argumento no se proporciona.
- **Listas de longitud variable** usando `...` (variádicos) para capturar múltiples argumentos en un array.
- **Argumentos con nombre** (PHP 8.0+) que permiten pasar valores asociándolos al nombre del parámetro, independientemente de la posición.
- **Comas finales** tanto en la lista de llamada (PHP 7.3+) como en la declaración de parámetros (PHP 8.0+) para mejorar la legibilidad en listados verticales [PHP](https://www.php.net/manual/en/functions.arguments.php).
### Definición
- Parámetros: Identificadores en la declaración de la función que reciben los valores de los argumentos.
- Argumentos: Expresiones pasadas en la llamada a la función, que se asignan a los parámetros correspondientes.
- La evaluación de argumentos ocurre antes de la llamada y es ansiosa (`eager evaluation`).
### Conceptos clave
- **Paso por valor vs referencia**:
    - Por defecto, PHP copia el valor al parámetro; con `function foo(&\$x)` se enlaza la variable original [PHP](https://www.php.net/manual/en/functions.arguments.php).
- **Valores por defecto** ([[6.3 Default & Optional Parameters]]):
    - `function bar(\$a = 1, \$b = "x")` usa el valor por defecto si no se pasa argumento (`null` no lo activa) [PHP](https://www.php.net/manual/en/functions.arguments.php).
    - Desde PHP 8.1.0, pueden ser objetos (`new ClassName()`) [PHP](https://www.php.net/manual/en/functions.arguments.php).
- **Parámetros opcionales** ([[6.3 Default & Optional Parameters]]):
    - Siempre tras los obligatorios; parámetros con defecto antes de uno obligatorio están **deprecados** desde PHP 8.0 [PHP](https://www.php.net/manual/en/functions.arguments.php).
- **Trailing comma**:
    - Llamadas a funciones (`foo(\$a, \$b,)`) desde PHP 7.3.0.
    - Declaración de parámetros (`function foo(\$a, \$b,)`) desde PHP 8.0.0 [PHP](https://www.php.net/manual/en/functions.arguments.php).
- **Listas variádicas** ([[6.9 Variadic Functions]]):
    - `function sum(...\$nums)` agrupa todos los argumentos extras en el array `\$nums` [PHP](https://www.php.net/manual/en/functions.arguments.php)
    - Se puede tipar: `function total(int ...\$vals): int` obliga a que cada argumento sea `int`.
- **Unpacking de argumentos**:    
    - En la llamada: `foo(...\$array)` expande los elementos del array como argumentos separados [PHP](https://www.php.net/manual/en/functions.arguments.php).
- **Named Arguments** ([[6.4 Named Arguments]]):
    - Sintaxis `foo(param2: 5, param1: 3)` para pasar valores por nombre, ignorando el orden y saltando opcionales [PHP](https://www.php.net/manual/en/functions.arguments.php).
    - No permitido redefinir el mismo parámetro o mezclar mal posicionados.
### Ejemplos prácticos
##### Paso por referencia
```php
<?php

function addExclam(&$s) {
	$s .= "!";
}

$text = "Hola";
addExclam($text);

echo $text; // "Hola!"

```
##### Valores por defecto y coma final
```php
<?php

function greet($name = "Mundo", $emoji = "😊",) {
	return "Hola, $name $emoji!";
}

echo greet();             // Hola, Mundo 😊
echo greet("Ana", "🚀");  // Hola, Ana 🚀

```
##### Invalidación de orden de parámetros (PHP 8.0+ deprecado)
```php
<?php
// Deprecated: parámetros opcionales antes de obligatorios
// function test($opt = 1, $req) { ... }
// Recomendado:
function test($req, $opt = 1) {...}

```
##### Variádicos y tipado
```php
<?php

function sum(int ...$nums): int {
	return array_sum($nums);
}

echo sum(1, 2, 3); // 6

```
##### Unpacking de argumentos
```php
<?php

function mul($a, $b) { return $a * $b }
$args = [2, 5];
echo mul(...$args); // 10

```
##### Named Arguments
```php
<?php

function makeTea($type = "negro", $size = "grande") {
	 return "$size té $type";
}

echo makeTea(size: "pequeño");                // pequeño té negro
echo makeTea(type: "verde", size: "mediano"); // mediano té verde

```
## Notas
---
- Usar `named arguments` para llamadas con muchos parámetros opcionales, pues mejora la legibilidad.
- Colocar parámetros **requeridos** antes de los opcionales para evitar deprecaciones.
- Utilizar variádicos con tipado para funciones de agregación (*sumas, concatenaciones*) y evitar índices manuales.