## Definición
`JSON Web Token (JWT)` es un estándar abierto (`RFC 7519`) que defina una forma compacta y autónoma de transmitir información entre dos partes de manera segura como un objeto `JSON`. Esta información puede ser verificada y confiable porque está firmada digitalmente. Los `JWT` pueden firmarse mediante un secreto (con el algoritmo `HMAC`) o un par de claves pública/privada usando `RSA` o `ECDSA`.

## Conceptos Clave
Un `JWT` se compone de tres partes separadas por puntos (`.`):
1. `Header (Encabezado)`: Contiene el tipo de token (`JWT`) y el algoritmo de firma utilizado (como `HS256` o `RS256`).
2. `Payload (Carga útil)`: Contiene los *`claims`* o declaraciones. Estas son piezas de información sobre una entidad (usualmente el usuario) y datos adiciones. Hay tres tipos de `claims`:
	- **Registrados**: Atributos estándar como `iss` (emisor), `exp` (expiración), `sub` (sujeto).
	- **Públicos**: Definidos por los usuarios del `token` (ej. nombres o correos).
	- **Privados**: Información personalizada compartida entre las partes.
3. **`Signature` (Firma)**: Se utiliza para verificar que el mensaje no fue cambiado en el camino. Se crea tomando el encabezado codificado, el `payload` codificado, un secreto y el algoritmo especificado.
### Flujo de Autenticación
El proceso típico de autenticación con `JWT` sigue estos pasos:
1. El usuario envía sus credenciales (usuario/contraseña).
2. El servidor valida las credenciales y genera un `JWT` firmado.
3. El servidor devuelve el `JWT` al cliente.
4. El cliente almacena el `token` (comúnmente en `localStorage` o una `HttpOnly Cookie`).
5. En cada solicitud posterior a recursos protegidos, el cliente envía el `token` en la cabecera `Authorization` usando el esquema `Bearer`.
6. El servidor verifica la firma del `token` y, si es válida, permite el acceso.

## Ejemplos Prácticos
##### Estructura de un `Token` (Codificado)
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.
eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyfQ.
SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c
```
##### Decodificación del `Payload` (`JSON`)
```json
{
  "sub": "1234567890",
  "name": "John Doe",
  "iat": 1516239022,
  "exp": 1716239022,
  "role": "admin"
}
```
##### Cabecera de Autorización
```http
Authorization: Bearer <token_aqui>
```

## Analogía
Imagina que un `JWT` es como un Billete de Avión Digital.
- El `Header` dice que es un billete oficial y qué sello de seguridad tiene.
- El `Payload` tienen tu nombre, número de asiento y hora de salida. Cualquiera puede leer estos datos si mira el billete.
- La firma es el sello holográfico de la aerolínea. Si intentas cambiar el número de asiento de "`32B`" a "`1A`", la firma ya no coincidirá con el contenido y el billete será rechazado en la puerta de embarque.
- La aerolínea no necesita buscarte en su base de datos en cada pasillo del aeropuerto; el billete mismo contiene toda la información necesaria para dejarte pasar.

## Buenas Prácticas
- **No guardes datos sensibles**: El `payload` solo está codificado en `Base64`, no cifrado. Nunca pongas contraseñas ni datos privados ahí.
- **Usa `HTTPS`**: Es obligatorio para evitar que alguien intercepte el `token` (ataque `Man-in-the-Middle`).
- **Tiempo de expiración corto**: Configura el campo `exp` para que el `token` dure poco tiempo (ej. 15 minutos) y usa *`Refresh Tokens`* para renovar el acceso.
- **Validación de algoritmos**: El servidor debe validar siempre el algoritmo de la cabecera para evitar ataque de tipo "`None algoritm`".
- **Secretos fuertes**: Usa claves de firma largas y complejas que sean imposibles de adivinar por fuerza bruta.
