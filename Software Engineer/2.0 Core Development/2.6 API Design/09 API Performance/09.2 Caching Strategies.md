## Definición
El `Caching de APIs` es la técnica de almacenar copias de respuestas de peticiones en una capa de almacenamiento de alta velocidad (como la memoria `RAM`) para servir futuras solicitudes de forma más rápida. El objetivo principal es reducir la latencia, ahorrar ancho de banda y disminuir la carga en los servidores de aplicaciones y bases de datos.

### Estrategias Principales de `Caching`
Para un arquitecto de software, elegir la estrategia adecuada depende de si prioriza la **velocidad de lectura** o la **consistencia de los datos**.
#### `Cache-Aside (Lazy Loading)`
Es la estrategia más común en aplicaciones web (muy frecuente en `frameworks` como Laravel). La aplicación es responsable de gestionar el caché.
- **Flujo**: La app busca en el caché. Si no está (`cache miss`), consulta la `DB`, guarda el resultado en el caché y lo devuelve.
- **Pros**: Resistente a fallos del caché (si cae, vas a la DB).
- **Contras**: La primera petición siempre es lenta.
#### `Read-Through Cache`
El caché se sitúa en medio de la aplicación y la base de datos. La aplicación siempre pregunta al caché.
- **Flujo**: Si el dato no está, el propio caché lo busca en la DB y se actualiza a sí mismo antes de responder.
- **Pros**: Simplifica el código de la aplicación.
- **Contras**: Requiere una configuración más compleja del lado del servidor de caché.
#### `Write-Throught Cache`
Los datos se escriben en el caché y en la base de datos al mismo tiempo.
- **Pros**: El caché siempre está actualizado con la última información. Consistencia garantizada.
- **Contras**: La escritura es más lenta porque debe ocurrir en dos lugares.
#### `Write-Back (Write-Behind)`
La aplicación escribe primero en el caché y confirma al usuario de inmediato. Luego, el caché actualiza la base de datos de forma asíncrona.
- **Pros**: Velocidad de escritura extrema. Ideal para cargas de datos masivas.
- **Contras**: Riesgo de pérdida de datos si el caché falla antes de sincronizar con la DB.

## Conceptos Críticos
- `TTL (Time To Live)`: El tiempo que un dato permanece en el caché antes de ser considerado "obsoleto" y eliminado.
- `Cache Invalidation`: El proceso de eliminar datos viejos cuando cambian en la DB. Es considerado uno de los problemas más difíciles en computación.
- `Eviction Policy (Políticas de Expulsión)`: Cómo decide el caché qué borrar cuando se llena (ej. `LRU - Least Recently Used`).
- `Headers de Caché (HTTP)`:
	- `Cache-Control`: Define si el navegador/`proxy` puede `cachear` la respuesta.
	- `ETag`: Un identificador de versión que permite al cliente preguntar: Ha cambiado este recurso?

## Analogía
Imagina que eres un Chef (Servidor) en un restaurante:
- Sin caché: Cada vez que alguien pide una ensalada, vas al huerto, recoges los ingredientes y los picas.
- `Cache-Aside`: La primera vez vas al huerto, pero picas de más y guardas una porción en un `Tupper (Caché)`. Si el siguiente cliente pide lo mismo, solo abres el `Tupper`.
- `Write-Through`: Cada vez que preparas una ensalada nueva, dejas una lista para el día siguiente y guardas una copia exacta en la nevera al mismo tiempo.

## Buenas Prácticas
- No caches todo: Enfócate en datos que cambian poco y se leen mucho (catálogos, configuraciones, perfiles públicos).
- Usa `Redis` o `Memcached`: Son los estándares de la industria para almacenamiento en memoria de alto rendimiento.
- Versatilidad de Claves: Diseña nombres de claves únicos y descriptivos (ej. `user:123:profile`) para evitar colisiones.
- Seguridad: Nunca caches datos altamente sensibles (`passwords`, `tokens` de sesión personales) en capas de caché compartidas o públicas (`proxies`).

## Notas Personales
- En el contexto de refactorización de un SaaS, implementar una capa de caché robusta puede reducir los costos de infraestructura a la mitad al liberar presión sobre la base de datos principal.
- La invalidación por eventos. En Laravel, usar `Observers` o `Events` para limpiar el caché en el momento exacto en que un modelo se actualiza.