### El Objeto `Request`: Acceso a Datos
Laravel utiliza la inyección de dependencias para proporcionar una instancia de `Illuminate\Http\Request` en tus controladores. Esta clase unifica el acceso a datos independientemente de si vienen de una cadena de consulta (`Query String`), el cuerpo de un formulario o una carga `JSON`.
##### Métodos de Recuperación de Entrada
- **`$request->all()`**: Recupera todos los datos de entrada como un array.
- **`$request->input('name', 'default')`**: Recupera un valor específico. Funciona tanto para parámetros de consulta como para el cuerpo de la petición.
- **`$request->query('id')`**: Accede exclusivamente a los parámetros de la URL (`?id=1`).
- **`$request->boolean('archived')`**: Convierte automáticamente valores como `"1"`, `"true"`, `"on"` o `"yes"` a un booleano real de PHP.
- **`$request->only(['username', 'password'])`**: Recupera solo un subconjunto de los datos (muy útil para seguridad).
- **`$request->except(['_token'])`**: Recupera todos los datos menos los especificados.
### Gestión de Archivos Subidos
Cuando un usuario envía un archivo (vía `multipart/form-data`), Laravel lo envuelve en una instancia de `Illuminate\Http\ UploadedFile`, que extiende la clase `File` de PHP con métodos mucho más convenientes.
##### Validación y Obtención
Antes de procesar, siempre se debe verificar la existencia del archivo:
```php
if ($request->hasFile('avatar') && $request->file('avatar')->isValid()) {
    $path = $request->avatar->store('avatars');
}
```
##### El Método `store()`
Es la forma más rápida y segura de guardar archivos. Laravel genera un `UUID` único para el nombre del archivo, lo cual previene colisiones y ataques de sobreescritura.
- `store('path', 'disk')`: Guarda el archivo en un directorio y disco específico (ej. `s3` o `public`).
### Recuperación de Archivos desde el `Filesystem`
Una vez almacenados, interactuar con los archivos requiere la fachada `Storage`. Laravel abstrae la ubicación física (`local`, `Amazon S3`, `FTP`) tras una configuración de "discos".
##### Operaciones Comunes
- **`Storage::get('file.jpg')`**: Obtiene el contenido crudo del archivo.
- **`Storage::exists('file.jpg')`**: Verifica la existencia.
- **`Storage::download('file.jpg')`**: Genera una respuesta de descarga para el navegador.
- **`Storage::url('file.jpg')`**: Obtiene la `URL` pública del archivo (si el disco está configurado para ello).
### Arquitectura: El Ciclo de Vida del `Request`
Es viral entender que el `Request` es inmutable en su mayor parte durante el ciclo de vida. Si necesitas modificar los datos antes de que lleguen al controlador, el lugar correcto es un `Middleware` o un `Form Request`.
##### `Form Requests (Senior Approach)`
Para mantener los controladores limpios, la validación y la autorización de los datos de la petición deben moverse a clases `FormRequest` dedicadas.
```bash
php artisan make:request StoreUserRequest
```

## Analogía
Imagina que el `Request` es un Recepcionista.
1. **`Retrieving Data`**: El cliente (Navegador) llega con maletas (Archivos), un formulario de registro (`Input`) y peticiones especiales en voz alta (`Query String`).
2. **`Validation`**: El recepcionista verifica que el cliente tenga una reserva válida y que sus maletas no contengan artículos prohibidos.
3. **`Storage`**: Las maletas pesadas se llevan a una Bodega de Seguridad (`Filesystem/S3`) y el recepcionista solo te entrega el número de ticket (`Path`) para que tú (Controlador) sepas dónde están.

## Buenas Prácticas
- **`Never trust $request->all()`**: Aunque sea cómodo, siempre usa `$request->validated()` (si usas `Form Request`) o `$request->only()` para asegurar que solo procesas lo que esperas.
- **Manejo de `JSON`**: Si la cabecera `Content-Type` es `application/json`, Laravel `parseará`  automáticamente el cuerpo. Puedes acceder a ellos igual que a los datos de un formulario.
- **Visibilidad de Archivos**: Al usar el disco `public`, recuerda que los archivos son accesibles por `URL`. Para archivos privados (como facturas), usa el disco `local` y sirve los archivos a través de un controlador que verifique permisos.
- **Uso de `old()`**: Cuando una validación falla, Laravel redirige atrás. Usa `$request->flash()` para mantener los datos en la sesión y la función `old('input_name')` en `Blade` para no obligar al usuario a rellenar el formulario otra vez.