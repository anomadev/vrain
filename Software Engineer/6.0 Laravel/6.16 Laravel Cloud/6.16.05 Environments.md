Las aplicaciones en Laravel Cloud pueden estar compuestas por muchos `environments` como `production`, `staging` o incluso entornos de corta duración que se utilizan para probar nuevas funciones. 
La infraestructura informática, los recursos y configuración de implementación son únicos para cada entorno. 

### Build and deploy commands
Desde la sección `Deployments` de la configuración del `environment` puede definir como se construye tanto el `frontend` como el `backend` de la aplicación durante el despliegue.
- **`Laravel`**: Normalmente, el comando de compilación instalará las dependencias de `Composer` y `npm` y compilará los recursos de su aplicación `composer install --no-dev && npm run build` .

> [!success] La ejecución de `php artisan optimize` o `php artisan config:cache` debe realizarse durante el proceso de compilación, no durante el proceso de despliegue.

### Deploy commands
Los comandos de despliegue de tu entorno se ejecutan en tu infraestructura justo antes de que un nuevo despliegue se active. A diferencia de los comandos de compilación, los cambios realizados en el sistema de archivos por los comando de despliegue no se guardarán en la aplicación.

> [!warning] Los comandos de despliegue deben completarse en un plazo de 15 minutos o caducarán.

Los siguientes comandos son gestionados automáticamente por Laravel Cloud o no deben ejecutarse durante el despliegue.
- `php artisan queue:restart`: se reinician automáticamente después de cada despliegue
- `php artisan horizon:teminate`: los procesos se gestionan automáticamente durante las implementaciones
- `php artisan optimize:clear`
- `php artisan storage:link`

### Variables de entorno
Dentro de la configuración del entorno, la sección de variables de entorno le permite personalizar las variables `.env` disponibles para su entorno. Además de sus variables, Laravel Cloud inyectará automáticamente una variedad de variables de entorno, incluyendo credenciales necesarias para conectarse a los recursos como base de datos, almacenamiento `key-value` o almacenamiento de objetos.

> [!info] Las variables de entorno personalizadas tienen prioridad y anularán cualquier variable inyectada automáticamente

Tras modificar las variable de un entorno, es necesario volver a desplegarlo para que los cambios surtan efecto.

### HTTP Basic authentication
La autenticación básica `HTTP` permite proteger un entorno con un nombre de usuario y una contraseña antes de que el tráfico llegue a su aplicación. Resulta útil para entornos de prueba, demostraciones o entornos en reposo que no deben activarse debido a solicitudes no autorizadas o tráfico automatizado de bots.
Puede habilitar la autenticación básica HTTP desde la página de `General Settings` en la sección `Security`.
La autenticación básica HTTP está disponible en el plan `Growth` o superior.

### Runtime
Laravel es compatible con PHP 8.2 a 8.5 para aplicaciones Laravel y Symfony (8.5 predeterminada para entornos nuevos). Puede seleccionar la versión que debe utilizar el entorno en la pagina `General Settings`. Tras el cambio es necesario volver a implementar la aplicación para que los cambios surtan efectos.

### Build add-ons
Las organizaciones `Enterprise` pueden instalar software adicional en su imagen de compilación. Esto resulta útil cuando la aplicación depende de una extensión PHP u otro software que no viene incluido por defecto.
Los complementos se configuran por entorno desde la sección `Build configuration`. Los complementos disponibles para su organización los determina Laravel y se muestran en el selector de `Add-ons`.

### PHP memory limit
Laravel Cloud asigna memoria PHP dinámicamente en función de un porcentaje de la memoria total de tu instancia. Si la aplicación requiere una cantidad específica, puedes modificar este límite definiendo un nuevo parámetro `memory_limit`.
Puedes anular el límite de memoria predeterminado agregando la configuración directamente en el código de tu aplicación. El método más común es actualizar tu archivo `public/index.php` y tu archivo `artisan`:

```php
<?php

// Set memory limit to 512MB
init_set('memory_limit', '512MB');

// remove memory limits entirely...
init_set('memory_limit', -1);
```

Puede establecer el límite de memoria mediante comandos de compilación para evitar modificar el código fuente de su proyecto:

```bash
# Add memory_limit to the top of the public/index.php file...
sed -i 's/<?php/<?php\nini_set('\''memory_limit'\'', '\''512M'\'');/g' public/index.php

# Add memory_limit to the top of the artisan file...
sed -i 's/<?php/<?php\nini_set('\''memory_limit'\'', '\''512M'\'');/g' artisan
```

El ajuste de memoria debe realizarse al inicio del ciclo de vida de la aplicación para garantizar un rendimiento óptimo. El uso excesivo de memoria puede afectar negativamente la eficiencia general de la aplicación.
Si bien establecer el límite de memoria en `-1` elimina todas las restricciones de memoria, esta opción debe usarse con precaución, especialmente en entornos de producción, ya que puede provocar un comportamiento impredecible.

### Cloud domains
Cada entorno recibe un dominio gratuito `laravel.cloud` la primera vez que se implementa correctamente. Los dominios se construyen utilizando una combinación del nombre de la aplicación y en nombre del entorno. Si se modifica alguno de estos nombres, el dominio se actualizará para reflejar los cambios. Para que este cambio surta efecto, es necesario un nuevo despliegue.

### Custom domains
Laravel Cloud te guía a través de la configuración de DNS, verificará la propiedad del dominio y emitirá un certificado `SSL` automáticamente.

### Stop and restart
Cuando se detiene un entorno, todos los clústeres de aplicaciones y trabajadores dentro de ese entorno se apagarán. Esto significa que se detendrá todo el tráfico web, los procesos en segundo plano y las tareas programadas. No se le cobrará por la capacidad de procesamiento del entorno, pero sí se le cobrará por cualquier recurso conectado.

### Replicate
Al crear nuevos entornos, suele ser más conveniente clonar un entorno existente y realizar cambios que crear un nuevo entorno desde cero. Por defecto, se duplicarán todos los ajustes generales, de entorno y de implementación. Opcionalmente, puede editar la configuración de replicación predeterminada para computación y recursos.
- `Compute`: duplica todos los clústeres (tamaño, escalado, configuración, etc.)
- `Database`: usar el mismo clúster de computación/almacenamiento para crear un nuevo esquema de base de datos lógico.
- `Cache`: usa la misma instancia pero agrega una variable de entorno `CACHE_PREFIX` única a su aplicación.
- `Object Storage`: sin replicación.

### Commands
Laravel Cloud te permite ejecutar comandos de `shell` arbitrarios directamente en tu entorno, en la pestaña `Commands` del entorno para ejecutar comandos de un solo uso.

### Filesystem
Los sistemas de archivos de entorno son efímeros, lo que significa que los archivos pueden no persistir entre solicitudes o trabajos. Las nuevas implementaciones o reimplementaciones reiniciarán el sistema de archivos. Además, cada réplica de su clúster de computación tiene su propio sistema de archivos. Por lo tanto, debe tratar el sistema de archivos como un espacio de disto temporal y no compartido que solo es consistente durante una única solicitud o tarea.
El espacio total en disco disponible en cada réplica es proporcional a la capacidad de procesamiento/RAM del clúster. Cada GB de RAM equivale a 512 MB de espacio en disco.

Para almacenamiento persistente de caché y sesiones, utilice los controladores de Redis o base de datos mediante un recurso `KV Store` o de base de datos. Para almacenamiento persistente de archivos, utilice `Object Storage`.

### Nightwatch integration
Los entornos de Laravel Cloud incluyen soporte integrado para la monitorización con `Laravel Nightwatch` que proporciona información detallada sobre el rendimiento, las excepciones, los registros y el estado general del entorno de la aplicación.

> [!warning] Si previamente configuró Nightwatch manualmente con un proceso en segundo plano, debe eliminar ese proceso de cada clúster antes de habilitar la integración para evitar problemas en su entorno.

