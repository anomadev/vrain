##### El estándar: Escapado Automático
La sintaxis más común en `Blade` es el doble uso de llaves. Por defecto, Laravel pasa el contenido por la función `htmlspacialchars` de PHP para prevenir ataques de `Cross-Site Scripting (XSS)`.
```php
// En tu archivo .blade.php
Hello, {{ $name }}.
```
**Nota de seguridad**: Siempre que uses `{{ }}`, Laravel asume que el contenido no es confiable y lo "limpia". Es tu red de seguridad por defecto.
##### Datos no escapados (`Unscaped Data`)
A veces, necesitas renderizar `HTML` real (por ejemplo, contenido proveniente de un editor de texto enriquecido). Para esto, se utiliza la sintaxis especial:
```php
// Imprime el HTML tal cual, sin escapar
{!! $content !!}
```
Advertencia: Utiliza esto únicamente con datos que tú mismo hayas generado o que hayan sido saneados previamente. Nunca imprimas directamente `inputs` del usuario con esta sintaxis.
##### Integración con `Frameworks` de `JavaScript`
Si utilizas `frameworks` como `Vue`, `Alpine` o `React` dentro de tus plantillas de `Blade`, habrá conflictos porque ambos usan llaves dobles. Para indicarle a `Blade` que ignore una expresión y la deje para el `frontend`, usamos el símbolo `@`:
- Individual: `@{{ name }}` Se renderiza como `{{ name }}` en el navegador.
- Bloque: Puedes usar la directiva `@verbatim` para envolver secciones grandes de código `JS`.
```html
@verbatim
    <div class="container">
        Hello, {{ name }}! </div>
@verbatim
```
##### Manejo de `JSON` y Atributos
Laravel facilita pasar datos del `backend` al `frontend` (como objetos de `Eloquent` a un `script` de `JS`) de forma segura.
Directiva `@json`: Convierte `arrays` u objetos de PHP en `strings JSON` válidos.
```html
<script>
    var appData = @json($array);
</script>
```
##### Atributos y Clases Condicionales (El `Senior Way`)
En lugar de usar operadores ternarios gigantes dentro de tus etiquetas `HTML`, Laravel introdujo directivas que limpian drásticamente el código.

| **Directiva** | **Propósito**                    | **Ejemplo**                                         |
| :------------ | :------------------------------- | :-------------------------------------------------- |
| `@class`      | Agrega clases CSS dinámicas      | `<div @class(['font-bold' => $isActive])></div>`    |
| `@style`      | Agregar estilos inline dinámicos | `<div @style(['color: red' => $hasError])></div>`   |
| `@checked`    | Marca un checkbox                | `<input type="checkbox" @checked(old('active')) />` |
| `@disabled`   | Deshabilita un elemento          | `<button @disabled($errors->any())>Submit</button>` |
## Buenas Prácticas
- **No proceses datos en `Blade`**: Si necesitas formatear un número o una fecha de forma compleja, hazlo en el `Controller` o usa un `Accessor` en el modelo. `Blade` solo debe mostrar, no calcular.
- **Usa `@json` con precaución**: Para volúmenes de datos muy grandes, es mejor cargar la información vía `API` para no engrosar el tamaño del archivo `HTML` inicial.
